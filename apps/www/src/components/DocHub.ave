<script lang="ts">
  export let groups
</script>

<template>
  <section>
    <h1 class="mb-4 mt-0 text-[clamp(1.75rem,4vw,2.25rem)] tracking-[-0.03em]">Docs</h1>
    {#each groups as g}
      <div class="mb-8">
        <h2 class="mb-3 mt-0 text-[1.15rem] tracking-[-0.02em]">{g.title}</h2>
        <ul class="m-0 flex list-none flex-col gap-4 p-0">
          {#each g.docs as d}
            <li>
              <a
                class="text-[1.05rem] font-bold text-fg underline underline-offset-[0.18em] hover:text-accent"
                href={'/docs/' + d.slug + '/'}
              >
                {d.title}
              </a>
              <p class="mt-1.5 mb-0 max-w-xl leading-snug text-muted">{d.blurb}</p>
            </li>
          {/each}
        </ul>
      </div>
    {/each}
  </section>
</template>
