<script lang="ts">
  export let prev
  export let next
</script>

<template>
  <nav
    class="mt-10 flex justify-between gap-4 border-t border-line pt-5"
    aria-label="Previous and next"
  >
    {#if prev}
      <a
        class="max-w-[45%] font-semibold text-accent underline underline-offset-[0.18em] hover:text-accent-deep"
        href={'/docs/' + prev.slug + '/'}
      >
        <span
          class="mb-1 block text-[0.75rem] font-bold uppercase tracking-[0.04em] text-muted"
        >
          Previous
        </span>
        {prev.title}
      </a>
    {/if}
    {#if next}
      <a
        class="ml-auto max-w-[45%] text-right font-semibold text-accent underline underline-offset-[0.18em] hover:text-accent-deep"
        href={'/docs/' + next.slug + '/'}
      >
        <span
          class="mb-1 block text-[0.75rem] font-bold uppercase tracking-[0.04em] text-muted"
        >
          Next
        </span>
        {next.title}
      </a>
    {/if}
  </nav>
</template>
