<script lang="ts">
  export let groups
  export let slug
</script>

<template>
  <aside
    class="self-start border-b border-line pb-4 md:sticky md:top-4 md:max-h-[calc(100vh-2rem)] md:overflow-auto md:border-b-0 md:border-r md:border-line md:pb-0 md:pr-4"
  >
    {#each groups as g}
      <div class="mb-7">
        <p
          class="mb-2.5 mt-0 border-b border-line pb-2 text-[0.8rem] font-extrabold tracking-[-0.01em] text-fg"
        >
          {g.title}
        </p>
        <nav class="flex flex-col gap-0.5 pl-0.5" aria-label={g.title}>
          {#each g.docs as d}
            <a
              href={'/docs/' + d.slug + '/'}
              class={
                d.slug === slug
                  ? 'flex min-h-10 items-center border-l-2 border-accent px-2.5 py-1.5 text-[0.9rem] font-medium text-accent no-underline'
                  : 'flex min-h-10 items-center border-l-2 border-transparent px-2.5 py-1.5 text-[0.9rem] font-normal text-muted no-underline hover:border-line hover:text-fg'
              }
            >
              {d.title}
            </a>
          {/each}
        </nav>
      </div>
    {/each}
  </aside>
</template>
