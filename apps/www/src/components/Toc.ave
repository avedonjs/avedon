<script lang="ts">
  export let headings
</script>

<template>
  <aside
    class="hidden self-start text-[0.85rem] lg:sticky lg:top-4 lg:block lg:max-h-[calc(100dvh-2rem)] lg:overflow-y-auto"
  >
    {#if headings.length}
      <p
        class="sticky top-0 z-10 mb-2 mt-0 bg-bg pb-1 text-[0.75rem] font-bold uppercase tracking-[0.04em] text-muted"
      >
        On this page
      </p>
      <nav class="flex flex-col gap-0.5" aria-label="On this page">
        {#each headings as h}
          <a
            href={'#' + h.id}
            class={
              h.level === 3
                ? 'flex items-center py-1 pl-3 leading-snug text-muted no-underline hover:text-fg'
                : 'flex items-center py-1 leading-snug text-muted no-underline hover:text-fg'
            }
          >
            {h.text}
          </a>
        {/each}
      </nav>
    {/if}
  </aside>
</template>
