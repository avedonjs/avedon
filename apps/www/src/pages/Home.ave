<script lang="ts">
  import { signal } from '@avedon/runtime'

  export let specimenHtml

  const selected = signal('npm')
  const cmd = signal('npm create avedon-app')
  const copied = signal(false)

  function pickNpm() {
    selected = 'npm'
    cmd = 'npm create avedon-app'
    copied = false
  }

  function pickPnpm() {
    selected = 'pnpm'
    cmd = 'pnpm create avedon-app'
    copied = false
  }

  function pickYarn() {
    selected = 'yarn'
    cmd = 'yarn create avedon-app'
    copied = false
  }

  function pickBun() {
    selected = 'bun'
    cmd = 'bun create avedon-app'
    copied = false
  }

  async function copyCmd() {
    try {
      await navigator.clipboard.writeText(cmd)
      copied = true
      setTimeout(() => (copied = false), 1600)
    } catch {
      copied = false
    }
  }
</script>

<script server>
  import { loadHomeSpecimenHtml } from '../lib/home-specimen'

  export async function load() {
    return {
      specimenHtml: loadHomeSpecimenHtml(),
      head: {
        title: 'avedon — TypeScript-first full-stack web framework',
        description:
          'avedon is a TypeScript-first full-stack web framework: single-file .ave components, explicit typed routing, and hybrid SSR/SSG/CSR rendering.',
      },
    }
  }
</script>

<style unscoped>
  .stage {
    position: relative;
    isolation: isolate;
    min-height: calc(100vh - 4rem);
    box-sizing: border-box;
    padding: clamp(1.75rem, 4vw, 3.5rem) clamp(1.25rem, 4vw, 3rem);
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
    grid-template-rows: auto auto auto;
    gap: clamp(1.25rem, 2.5vw, 2rem) clamp(1.5rem, 4vw, 3.5rem);
    align-items: start;
    width: min(100%, 72rem);
    margin: 0 auto;
    overflow: visible;
  }

  .stage-glow {
    position: absolute;
    top: -8%;
    right: -6%;
    width: min(72vw, 40rem);
    height: min(72vw, 40rem);
    background: radial-gradient(circle, rgba(6, 182, 212, 0.18), transparent 68%);
    pointer-events: none;
    z-index: -1;
    will-change: transform;
    animation: drift 18s ease-in-out infinite alternate;
  }

  .stage-glow-2 {
    position: absolute;
    bottom: -18%;
    left: -12%;
    width: min(55vw, 28rem);
    height: min(55vw, 28rem);
    background: radial-gradient(circle, rgba(34, 211, 238, 0.08), transparent 70%);
    pointer-events: none;
    z-index: -1;
    animation: drift 22s ease-in-out infinite alternate-reverse;
  }

  .stage-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(250, 250, 250, 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(250, 250, 250, 0.035) 1px, transparent 1px);
    background-size: 44px 44px;
    mask-image: radial-gradient(ellipse at 28% 18%, #000 18%, transparent 72%);
    pointer-events: none;
    z-index: -1;
  }

  .stage-beam {
    position: absolute;
    top: 12%;
    left: -10%;
    width: 55%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(6, 182, 212, 0.35), transparent);
    pointer-events: none;
    z-index: -1;
    transform: rotate(-12deg);
    animation: beam 9s ease-in-out infinite alternate;
  }

  .crop {
    position: absolute;
    width: 1.35rem;
    height: 1.35rem;
    pointer-events: none;
    z-index: 0;
    opacity: 0.55;
    color: var(--accent);
    animation: crop-in 1s ease both;
  }

  .crop::before,
  .crop::after {
    content: '';
    position: absolute;
    background: currentColor;
  }

  .crop-tl {
    top: 1rem;
    left: 1rem;
  }

  .crop-tl::before {
    top: 0;
    left: 0;
    width: 100%;
    height: 1px;
  }

  .crop-tl::after {
    top: 0;
    left: 0;
    width: 1px;
    height: 100%;
  }

  .crop-tr {
    top: 1rem;
    right: 1rem;
    animation-delay: 0.08s;
  }

  .crop-tr::before {
    top: 0;
    right: 0;
    width: 100%;
    height: 1px;
  }

  .crop-tr::after {
    top: 0;
    right: 0;
    width: 1px;
    height: 100%;
  }

  .crop-bl {
    bottom: 1rem;
    left: 1rem;
    animation-delay: 0.12s;
  }

  .crop-bl::before {
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
  }

  .crop-bl::after {
    bottom: 0;
    left: 0;
    width: 1px;
    height: 100%;
  }

  .crop-br {
    bottom: 1rem;
    right: 1rem;
    animation-delay: 0.16s;
  }

  .crop-br::before {
    bottom: 0;
    right: 0;
    width: 100%;
    height: 1px;
  }

  .crop-br::after {
    bottom: 0;
    right: 0;
    width: 1px;
    height: 100%;
  }

  .brand {
    grid-column: 1 / -1;
    grid-row: 1;
    margin: 0;
    font-size: clamp(3.75rem, 11vw, 7.25rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 0.95;
    padding-right: 0.06em;
    position: relative;
    z-index: 2;
    animation: rise 0.7s ease both;
  }

  .copy {
    grid-column: 1;
    grid-row: 2;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1.25rem;
    min-width: 0;
    position: relative;
    z-index: 1;
  }

  .headline {
    margin: 0;
    max-width: 16ch;
    font-size: clamp(1.4rem, 3.2vw, 2.15rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.12;
    animation: rise 0.7s ease 0.06s both;
  }

  .support {
    margin: 0;
    max-width: 34rem;
    color: var(--muted);
    font-size: 1.05rem;
    font-weight: 400;
    line-height: 1.55;
    animation: rise 0.7s ease 0.1s both;
  }

  .traits {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.8rem;
    line-height: 1.4;
    color: var(--muted);
    animation: rise 0.7s ease 0.14s both;
  }

  .traits li {
    display: flex;
    gap: 0.65rem;
    align-items: baseline;
  }

  .traits .mark {
    color: var(--accent);
    flex: 0 0 auto;
  }

  .install {
    display: flex;
    flex-direction: column;
    max-width: min(100%, 34rem);
    border: 1px solid var(--line);
    overflow: hidden;
    animation: rise 0.7s ease 0.16s both;
  }

  .install-tabs {
    display: flex;
    gap: 0;
    border-bottom: 1px solid var(--line);
    background: rgba(250, 250, 250, 0.02);
  }

  .install-tabs button {
    font: inherit;
    font-weight: 600;
    font-size: 0.8rem;
    padding: 0.45rem 0.85rem;
    border: none;
    background: transparent;
    color: var(--muted);
    cursor: pointer;
    border-bottom: 2px solid transparent;
    margin-bottom: -1px;
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .install-tabs button:hover {
    color: var(--fg);
  }

  .install-tabs button.active {
    color: var(--accent);
    border-bottom-color: var(--accent);
  }

  .install-row {
    display: flex;
    align-items: stretch;
  }

  .install-row code {
    flex: 1;
    margin: 0;
    padding: 0.7rem 1rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.9rem;
    line-height: 1.4;
    color: var(--fg);
    background: rgba(250, 250, 250, 0.03);
    overflow-x: auto;
  }

  .install-row > button {
    font: inherit;
    font-weight: 700;
    font-size: 0.85rem;
    padding: 0.7rem 1rem;
    border: none;
    border-left: 1px solid var(--line);
    background: transparent;
    color: var(--accent);
    cursor: pointer;
    white-space: nowrap;
    transition: color 0.15s ease, background 0.15s ease;
  }

  .install-row > button:hover {
    color: var(--accent-deep);
    background: rgba(6, 182, 212, 0.08);
  }

  .ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    animation: rise 0.7s ease 0.2s both;
  }

  .cta-primary,
  .cta-secondary {
    font: inherit;
    font-weight: 700;
    font-size: 0.95rem;
    padding: 0.65rem 1.1rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    text-decoration: none;
  }

  .cta-primary {
    background: var(--accent);
    color: #042f2e;
    border: 1px solid transparent;
  }

  .cta-primary:hover {
    background: var(--accent-deep);
    color: #042f2e;
  }

  .cta-secondary {
    border: 1px solid var(--line);
    color: var(--fg);
    background: transparent;
  }

  .cta-secondary:hover {
    border-color: var(--accent);
    color: var(--accent);
  }

  .specimen {
    grid-column: 2;
    grid-row: 2;
    position: relative;
    min-width: 0;
    align-self: stretch;
    z-index: 0;
    animation: rise 0.8s ease 0.12s both;
  }

  .specimen-frame {
    position: relative;
    border: 1px solid var(--line);
    background:
      linear-gradient(160deg, rgba(250, 250, 250, 0.03), transparent 42%),
      #0c0c0e;
    overflow: hidden;
  }

  .specimen-bar {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.65rem 0.85rem;
    border-bottom: 1px solid var(--line);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.75rem;
    color: var(--muted);
  }

  .specimen-dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: rgba(250, 250, 250, 0.18);
    flex: 0 0 auto;
  }

  .specimen-dot:nth-child(1) {
    background: #f87171;
  }

  .specimen-dot:nth-child(2) {
    background: #fbbf24;
  }

  .specimen-dot:nth-child(3) {
    background: #34d399;
  }

  .specimen-file {
    margin-left: 0.25rem;
    color: var(--fg);
    opacity: 0.85;
  }

  .specimen-code {
    margin: 0;
    overflow-x: auto;
  }

  .specimen-code pre.shiki {
    margin: 0;
    padding: 1.1rem 1.15rem 1.35rem;
    background: transparent !important;
    overflow-x: auto;
    font-size: clamp(0.72rem, 1.4vw, 0.84rem);
    line-height: 1.55;
  }

  .specimen-code pre.shiki code {
    display: block;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: inherit;
    background: transparent;
    padding: 0;
    white-space: pre;
  }

  .specimen-glow {
    position: absolute;
    inset: auto -20% -35% auto;
    width: 70%;
    height: 70%;
    background: radial-gradient(circle, rgba(6, 182, 212, 0.14), transparent 65%);
    pointer-events: none;
    z-index: -1;
  }

  .footer-line {
    grid-column: 1 / -1;
    grid-row: 3;
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.5rem;
    align-items: center;
    padding-top: 0.25rem;
    border-top: 1px solid var(--line);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.75rem;
    color: var(--muted);
    animation: rise 0.7s ease 0.24s both;
  }

  .footer-line span {
    display: inline-flex;
    gap: 0.4rem;
    align-items: center;
  }

  .footer-line .sep {
    color: rgba(250, 250, 250, 0.2);
  }

  @keyframes rise {
    from {
      opacity: 0;
      transform: translateY(0.6rem);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @keyframes drift {
    0% {
      transform: translate(8%, -18%) scale(1);
    }
    33% {
      transform: translate(-55%, 35%) scale(1.08);
    }
    66% {
      transform: translate(-15%, 70%) scale(0.92);
    }
    100% {
      transform: translate(-70%, -5%) scale(1.05);
    }
  }

  @keyframes beam {
    from {
      opacity: 0.35;
      transform: rotate(-12deg) translateX(-2%);
    }
    to {
      opacity: 0.8;
      transform: rotate(-12deg) translateX(6%);
    }
  }

  @keyframes crop-in {
    from {
      opacity: 0;
      transform: scale(0.7);
    }
    to {
      opacity: 0.55;
      transform: none;
    }
  }

  @media (max-width: 900px) {
    .stage {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
      align-items: start;
      overflow: visible;
    }

    .brand,
    .copy,
    .specimen,
    .footer-line {
      grid-column: 1;
      grid-row: auto;
    }

    .headline {
      max-width: 22ch;
    }

    .crop {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .brand,
    .headline,
    .support,
    .traits,
    .install,
    .ctas,
    .specimen,
    .footer-line,
    .crop {
      animation: none;
    }

    .stage-glow,
    .stage-glow-2,
    .stage-beam {
      animation: none;
    }

    .stage-glow {
      transform: translate(8%, -18%);
    }
  }
</style>

<template>
  <div class="stage" data-www-stage>
    <div class="stage-glow" aria-hidden="true"></div>
    <div class="stage-glow-2" aria-hidden="true"></div>
    <div class="stage-grid" aria-hidden="true"></div>
    <div class="stage-beam" aria-hidden="true"></div>
    <span class="crop crop-tl" aria-hidden="true"></span>
    <span class="crop crop-tr" aria-hidden="true"></span>
    <span class="crop crop-bl" aria-hidden="true"></span>
    <span class="crop crop-br" aria-hidden="true"></span>

    <p class="brand">avedon</p>

    <div class="copy">
      <h1 class="headline">TypeScript-first full-stack web framework.</h1>
      <p class="support">
        One .ave file for UI, styles, and server logic — explicit routes, hybrid rendering, Vite
        toolchain.
      </p>
      <ul class="traits" aria-label="Highlights">
        <li><span class="mark">▸</span><span>.ave components — script, style, template in one file</span></li>
        <li><span class="mark">▸</span><span>Typed routes with load, actions, and streaming SSR</span></li>
        <li><span class="mark">▸</span><span>Hybrid SSR / SSG / CSR — pick per route</span></li>
      </ul>
      <div class="install">
        <div class="install-tabs" role="tablist" aria-label="Package manager">
          <button type="button" role="tab" class={selected === 'npm' ? 'active' : ''} on:click={pickNpm}>npm</button>
          <button type="button" role="tab" class={selected === 'pnpm' ? 'active' : ''} on:click={pickPnpm}>pnpm</button>
          <button type="button" role="tab" class={selected === 'yarn' ? 'active' : ''} on:click={pickYarn}>yarn</button>
          <button type="button" role="tab" class={selected === 'bun' ? 'active' : ''} on:click={pickBun}>bun</button>
        </div>
        <div class="install-row">
          <code>{cmd}</code>
          <button type="button" on:click={copyCmd}>{copied ? 'Copied' : 'Copy'}</button>
        </div>
      </div>
      <div class="ctas">
        <a class="cta-primary" href="/docs/quick-start/">Get started</a>
        <a class="cta-secondary" href="/playground">Open playground</a>
        <a class="cta-secondary" href="https://github.com/avedonjs/avedon" rel="noopener noreferrer" target="_blank">GitHub</a>
      </div>
    </div>

    <aside class="specimen" aria-label="Sample .ave component">
      <div class="specimen-glow" aria-hidden="true"></div>
      <div class="specimen-frame">
        <div class="specimen-bar">
          <span class="specimen-dot" aria-hidden="true"></span>
          <span class="specimen-dot" aria-hidden="true"></span>
          <span class="specimen-dot" aria-hidden="true"></span>
          <span class="specimen-file">Counter.ave</span>
        </div>
        <div class="specimen-code">{@html specimenHtml}</div>
      </div>
    </aside>

    <p class="footer-line">
      <span>This website is built with avedon.</span>
      <span class="sep" aria-hidden="true">/</span>
      <span>compiler · runtime · server</span>
      <span class="sep" aria-hidden="true">/</span>
      <span>adapters: node · bun · cloudflare</span>
      <span class="sep" aria-hidden="true">/</span>
      <span>open source</span>
    </p>
  </div>
</template>
