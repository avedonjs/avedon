<script lang="ts">
  import DocsSidebar from '../components/DocsSidebar.ave'
  import DocHub from '../components/DocHub.ave'

  export let groups
</script>

<script server>
  import { listNavGroups } from '../lib/docs'

  export async function load() {
    return {
      groups: listNavGroups(true),
      head: {
        title: 'Docs — avedon',
        description:
          'Guides and API reference for avedon: routing, rendering, data loading, components, and deployment.',
      },
    }
  }
</script>

<template>
  <div
    class="mx-auto grid w-[min(100%,72rem)] box-border grid-cols-1 gap-8 px-[clamp(1rem,3vw,2rem)] py-6 pb-12 md:grid-cols-[minmax(12rem,16rem)_minmax(0,1fr)]"
  >
    <DocsSidebar groups={groups} />
    <DocHub groups={groups} />
  </div>
</template>
