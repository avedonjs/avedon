<script lang="ts">
  import DocsSidebar from '../components/DocsSidebar.ave'
  import DocHub from '../components/DocHub.ave'

  export let groups
</script>

<script server>
  import { listNavGroups } from '../lib/docs'

  export async function load() {
    return {
      groups: listNavGroups(true),
      head: {
        title: 'Docs — avedon',
        description: 'Guides and API reference for avedon: routing, rendering, data loading, components, and deployment.',
      },
    }
  }
</script>

<style scoped>
  .docs-shell {
    display: grid;
    grid-template-columns: minmax(12rem, 16rem) minmax(0, 1fr);
    gap: 2rem;
    width: min(100%, 72rem);
    margin: 0 auto;
    padding: 1.5rem clamp(1rem, 3vw, 2rem) 3rem;
    box-sizing: border-box;
  }

  @media (max-width: 720px) {
    .docs-shell {
      grid-template-columns: 1fr;
    }
  }
</style>

<template>
  <div class="docs-shell">
    <DocsSidebar groups={groups} />
    <DocHub groups={groups} />
  </div>
</template>
