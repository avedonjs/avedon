# Plan: Avedon `.ave` LSP (diagnostics v1)

Updated: 2026-07-31
**Spec:** `docs/superpowers/specs/2026-07-31-ave-lsp-design.md`

## Steps

1. Compiler: `SourceRange` / `CompileError` / section `ranges` / `markupFileOffset` + `diagnoseAve` + unit tests
2. `@avedon/language-server`: stdio, debounce didOpen/didChange → `publishDiagnostics`
3. `avedon-vscode`: language contribution, grammar, language client, VSIX/dev docs
4. Spec, README/config note, changeset, memories

## Done when

- Invalid `.ave` templates produce diagnostics with absolute offsets
- Language server maps them to LSP ranges
- Extension builds and documents local VSIX install
