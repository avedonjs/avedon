<script lang="ts">
  import { signedDecimal, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>signedDecimal lab</h1>
    <input data-signed-decimal type="text" use:signedDecimal on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
