<script lang="ts">
  import { url, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>url lab</h1>
    <input data-url type="text" use:url on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
