<script lang="ts">
  import { cvv, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>cvv lab</h1>
    <input data-cvv type="text" use:cvv on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
