<script lang="ts">
  import { signal, trainCase } from '@avedon/runtime'

  const out = signal('idle')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>trainCase lab</h1>
    <input data-train-case type="text" use:trainCase on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>

