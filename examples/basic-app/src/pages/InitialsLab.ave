<script lang="ts">
  import { initials, signal } from '@avedon/runtime'

  const out = signal('idle')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>initials lab</h1>
    <input data-initials type="text" use:initials on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>

