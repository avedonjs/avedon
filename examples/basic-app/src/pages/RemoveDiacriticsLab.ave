<script lang="ts">
  import { removeDiacritics, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>removeDiacritics lab</h1>
    <input data-remove-diacritics type="text" use:removeDiacritics on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>

