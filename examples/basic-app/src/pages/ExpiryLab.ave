<script lang="ts">
  import { expiry, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>expiry lab</h1>
    <input data-expiry type="text" use:expiry on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
