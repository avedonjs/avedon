<script lang="ts">
  import { alphanumeric, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>alphanumeric lab</h1>
    <input data-alphanumeric type="text" use:alphanumeric on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
