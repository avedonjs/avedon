<script lang="ts">
  import { constantCase, signal } from '@avedon/runtime'

  const out = signal('idle')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>constantCase lab</h1>
    <input data-constant-case type="text" use:constantCase on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>

