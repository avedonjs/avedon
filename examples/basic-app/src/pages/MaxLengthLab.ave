<script lang="ts">
  import { maxLength, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>maxLength lab</h1>
    <input data-maxlength type="text" use:maxLength={5} on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
