<script lang="ts">
  import { ascii, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>ascii lab</h1>
    <input data-ascii type="text" use:ascii on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
