<script lang="ts">
  import { otp, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>otp lab</h1>
    <input data-otp type="text" use:otp on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
