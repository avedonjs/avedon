<script lang="ts">
  import { iban, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>iban lab</h1>
    <input data-iban type="text" use:iban on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
