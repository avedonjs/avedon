<script lang="ts">
  import { numeric, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>numeric lab</h1>
    <input data-numeric type="text" use:numeric on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
