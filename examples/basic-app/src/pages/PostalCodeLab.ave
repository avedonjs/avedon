<script lang="ts">
  import { postalCode, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>postalCode lab</h1>
    <input data-postal-code type="text" use:postalCode on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
