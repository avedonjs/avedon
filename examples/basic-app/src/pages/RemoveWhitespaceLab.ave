<script lang="ts">
  import { removeWhitespace, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>removeWhitespace lab</h1>
    <input data-remove-whitespace type="text" use:removeWhitespace on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
