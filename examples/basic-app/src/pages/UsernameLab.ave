<script lang="ts">
  import { username, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>username lab</h1>
    <input data-username type="text" use:username on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
