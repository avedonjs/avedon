<script lang="ts">
  import { integer, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>integer lab</h1>
    <input data-integer type="text" use:integer on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
