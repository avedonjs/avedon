<script lang="ts">
  import { pin, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>pin lab</h1>
    <input data-pin type="password" use:pin on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
