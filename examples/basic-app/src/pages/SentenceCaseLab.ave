<script lang="ts">
  import { sentenceCase, signal } from '@avedon/runtime'

  const out = signal('idle')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>sentenceCase lab</h1>
    <input data-sentence-case type="text" use:sentenceCase on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>

