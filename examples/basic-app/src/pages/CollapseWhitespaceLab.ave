<script lang="ts">
  import { collapseWhitespace, signal } from '@avedon/runtime'

  const out = signal('')

  function onInput(e: Event) {
    const t = e.target as HTMLInputElement
    out.set(t.value)
  }
</script>

<template>
  <main>
    <h1>collapseWhitespace lab</h1>
    <input data-collapse-whitespace type="text" use:collapseWhitespace on:input={onInput} />
    <p data-out>{out.get()}</p>
  </main>
</template>
