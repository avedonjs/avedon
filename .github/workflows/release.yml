name: Release

on:
  push:
    branches: [main, master]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Version & publish
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      # Trusted publishing (OIDC) needs Node ≥22.14 and npm ≥11.5.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      # Trusted publishing needs npm ≥11.5.1. Do NOT use npm@latest (12+):
      # pnpm 9 forwards --git-checks to npm publish; npm 12 throws EUNKNOWNCONFIG.
      - name: Install npm 11 for publish (OIDC + git-checks compat)
        run: npm install -g npm@11

      - run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build

      # Prefer OIDC trusted publishing. Optional NPM_TOKEN is a fallback only
      # (GAT bypass2fa is deprecated — see npm changelog 2026-07-08).
      - name: Create Release Pull Request or publish to npm
        uses: changesets/action@v1
        with:
          version: pnpm changeset version
          publish: pnpm changeset publish
          commit: 'chore: version packages'
          title: 'chore: version packages'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
