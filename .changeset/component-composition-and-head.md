---
"@avedon/compiler": minor
"@avedon/shared": minor
"@avedon/server": minor
"@avedon/vite-plugin": patch
"avedon": patch
---

Add reusable `.ave` component composition (PascalCase tags, props, default slots, fail-closed unsupported syntax) and per-page document head from `load` (`head: { title, description, html }` with streaming `awaitHead`).
