---
'@avedon/runtime': minor
'@avedon/compiler': patch
---

Add component context: `setContext` / `getContext` / `hasContext` with `__contextBegin` frames on SSR `render` / `renderInto` and client `mount`.
